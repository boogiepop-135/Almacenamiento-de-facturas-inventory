[providers]
node = "18"

[phases.setup]
nixPkgs = { nodejs = "18" }

[phases.install]
cmds = [
  "cd ../frontend && npm ci",
  "npm ci"
]

[phases.build]
cmds = [
  "cd ../frontend && npm run build",
  "echo 'Backend build complete'"
]

[start]
cmd = "npm start"

